namespace GameLibrary {
  // Will be used in Character.cs to define the movement directions in a case statement. Just why would you ever do this...
  public enum MoveDir {
    NO_MOVE,
    UP,
    DOWN,
    LEFT,
    RIGHT
  }
}